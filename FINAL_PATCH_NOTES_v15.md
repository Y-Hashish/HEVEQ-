# FINAL PATCH NOTES v15

## التعديلات

1. أضفنا رابط المحادثات في Sidebar الخاص بالعميل.
2. أضفنا رابط الملف الشخصي في Sidebar الخاص بالمزود حتى يقدر المزود الجديد يكمل بيانات الشركة وموقع الخدمة.
3. عدلنا Dashboard العميل بحيث حالة التوثيق تعتمد على وجود بطاقة شخصية NationalId معتمدة، وليس على قيمة `RequiresAdditionalVerification` فقط.
4. عدلنا Backend middleware الخاص بتقييد الحسابات بحيث:
   - الحساب غير النشط لا يقدر ينفذ أي إجراءات عملية.
   - العميل أو المزود غير الموثق ببطاقة شخصية معتمدة لا يقدر يحجز أو يشتري أو يضيف خدمات أو ينفذ إجراءات مزود.
   - يظل مسموح له بالتصفح والبحث، رفع مستندات التوثيق، تحديث الملف الشخصي، والتواصل مع الدعم.
5. عدلنا Booking Create Context بحيث يرجع بيانات نطاق المزود، ويظهر للعميل تحذير قبل إرسال الحجز إذا كان العنوان خارج نطاق خدمة المزود.
6. عدلنا إدخال العناوين في الفرونت:
   - المستخدم لم يعد يكتب Latitude و Longitude يدوياً.
   - المستخدم يلصق رابط الخريطة، والفرونت يستخرج الإحداثيات ويرسلها للباك.
   - التعديل شمل العناوين المحفوظة، العنوان اليدوي في إنشاء الحجز، وموقع مقر المزود داخل الملف الشخصي.

## التحقق

- تم تشغيل TypeScript check بنجاح:

```bash
cd Frontend
./node_modules/.bin/tsc -p tsconfig.app.json --noEmit
```

- لم يتم تشغيل dotnet build داخل هذه البيئة لأن .NET SDK غير متاح.

## ملاحظات مهمة

- روابط Google Maps المختصرة جداً قد لا تحتوي على الإحداثيات داخل النص نفسه، والمتصفح لا يستطيع فكها بدون طلب خارجي. الأفضل نسخ رابط يحتوي على `@lat,lng` أو `q=lat,lng` أو لصق الإحداثيات بالشكل `30.123,31.456`.
- لم تتم إضافة migrations جديدة.
